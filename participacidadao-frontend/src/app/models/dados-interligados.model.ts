export interface DadosInterligados {
    id?: number;
    cityName: string;
    population?: number;
    area?: number; // in square kilometers
    dbpediaUri?: string;
    wikidataUri?: string;
    pibPerCapita?: number; // in BRL
}